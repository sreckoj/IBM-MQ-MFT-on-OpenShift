apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-srcagent
  namespace: mq
spec:
  storageClassName: managed-nfs-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi